name: tlir
channels:
  - defaults
dependencies:
  - python=3.11
  - matplotlib
  - numpy
  - pandas
  - pip
  - pytest
  - ipywidgets
  - rich
  - setuptools
  - pyrsistent
  - tqdm
  - pip:
      - plyfile
      - mitsuba
